from setuptools import setup, find_packages

setup(
    name='receiver',
    version='0.0.1',
    author='Author',
    author_email='Email',
    description='Package description',
    packages=find_packages()
)